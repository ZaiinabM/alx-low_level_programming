#include <stdio.h>
/**
 * main- entry
 * Return: always 0 (success)
 */
int main(void)
{
	char x;
	char y;

	for (x = 'a'; x <= 'z'; x++)
	{
		putchar(x);
	}
	for (y = 'Z'; y => 'A'; y--)
	{
		putchar(y);
	}
	putchar('\n');
	Return(0);
